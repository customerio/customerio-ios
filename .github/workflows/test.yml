name: Tests

on:
  pull_request:
    paths-ignore:
      - '**/*.md'
  push:
    branches: [main] # code coverage reporting needs to be on main so all PRs can compare against it. 

concurrency: # cancel previous workflow run if one exists. 
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: customerio/mobile-ci-tools/.github/workflows/ios-test.yml@main
    with:
      # Run on GitHub's Xcode 27 image (macOS 27). macos-26 tops out at Xcode 26.6, so it cannot
      # build/test against the iOS 27 SDK. `xcode-27` is GitHub's public-preview image today (not
      # yet GA) — revisit once promoted. Only one Xcode is installed on that image, so leave
      # xcode-version empty: the shared workflow then skips setup-xcode and uses the image default.
      runner-label: 'xcode-27'
      xcode-version: ''
      scheme: 'Customer.io-Package'
      xcresult-name: 'Customer.io-Package.xcresult'
      # Must track the Ruby this repo's Gemfile.lock is resolved against; the lock pulls
      # excon 1.7, which needs >= 3.3, so the shared workflow's 3.2 default cannot install it.
      ruby-version: '3.4'
    secrets:
      CODECOV_UPLOAD_TOKEN: ${{ secrets.CODECOV_UPLOAD_TOKEN }}  
  